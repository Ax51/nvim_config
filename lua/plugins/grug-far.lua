return {
  'MagicDuck/grug-far.nvim',

  cmd = "GrugFar",

  opts = {
    folding = {
      -- NOTE: this option prevents this plugin from loading without call (lazy loading)
      enabled = false
    }
  },
}
